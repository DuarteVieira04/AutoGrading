; Read by some PHP SAPIs when the document root is `public/`.
; Restart PHP-FPM / Apache after changing. For `php artisan serve`, use: composer run serve
upload_max_filesize = 512M
post_max_size = 520M
